<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="INFT2051.HomePage"
             Title="Home">

    <Grid Padding="30"
          RowDefinitions="Auto,Auto,*,Auto"
          ColumnDefinitions="*,Auto">

        <!-- Settings button -->
        <Button Text="Settings"
                Grid.Row="0"
                Grid.Column="1"
                Clicked="OnSettingsClicked"/>

        <!-- title -->
        <Label Text="My Diary"
               FontSize="32"
               HorizontalOptions="Center"
               Grid.Row="1"
               Grid.ColumnSpan="2" />

        <!-- Recent Diaries -->
        <VerticalStackLayout Grid.Row="2"
                             Grid.ColumnSpan="2"
                             Spacing="15">

            <Label Text="Recent Diaries"
                   FontSize="22"/>

            <CollectionView x:Name="RecentDiaryCollection"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Stroke="Gray"
                                StrokeThickness="1"
                                Padding="10"
                                Margin="0,0,0,10">
                            <VerticalStackLayout Spacing="8">

                                <Label Text="{Binding Title}"
                                       FontSize="18"
                                       FontAttributes="Bold"/>

                                <Label Text="{Binding Date}"
                                       FontSize="13"
                                       TextColor="Gray"/>

                                <Label Text="{Binding Preview}"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="2"/>

                                <Button Text="Edit"
                                        HorizontalOptions="End"
                                        Clicked="OnEditClicked"
                                        CommandParameter="{Binding Id}"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>

        <!-- put create diary on the bottom -->
        <Button Text="Create New Diary"
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Margin="0,20,0,0"
                Clicked="OnCreateDiaryClicked"/>

    </Grid>

</ContentPage>